

Select b.description as category_name, a.price,a.quantity,a.department,a.description
from data_entry_category b, data_entry a
where a.category = b.category

select b.description as category_name,a.price,a.quantity,a.department,a.description
from data_entry_category b, data_entry a
where a.category = b.category

